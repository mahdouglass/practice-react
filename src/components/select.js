import React from "react";

const Select = (props) => {
    return(
        <div className="form-group">
            <select>
                <option></option>
            </select>
        </div>
    )
}