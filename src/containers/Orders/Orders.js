import React, { Component } from 'react';

class Orders extends Component {
    render () {
        return (
            <React.Fragment>
                
            </React.Fragment>
        );
    }
}

export default Orders;